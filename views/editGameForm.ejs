<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>

        :root {
        --blue-600: #155dfc;
        --neutral-100: #f5f5f5;
        --zinc-950: #09090b;
        --blue-800: #193cb8;
        --blue-400: #51a2ff;
        --red-400: #ff6467;
        --red-600: #e7000b;
        --red-900: #82181a;
        }

        body {
            display: grid;
            place-items: center;
            min-height: 100vh;
            grid-template-columns: 1fr;
        }

        form {
            width: 80%;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        }

        fieldset {
            display: flex;
            flex-direction: column;
            gap: 1em;
            border: 2px solid var(--blue-400);
            border-radius: 5px;
            padding: 1rem;
        }

        legend {
            font-size: 1.4rem;
            font-family: Georgia, 'Times New Roman', Times, serif;
            color: var(--blue-800);
        }

        fieldset > div {
            display: flex;
            flex-direction: column;
            max-width: max(60%, 100px);
        }

        fieldset > button:nth-of-type(1) {
        color: var(--blue-600);
        text-decoration: none;
        font-weight: 700;
        font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        padding: 10px 20px;
        border: 2px solid var(--blue-600);
        border-radius: 5px;
        align-self: center;
      }

      select {
        border: 1px solid var(--blue-600);
        border-radius: 5px;
        height: 2rem;
        font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        font-size: 1.2rem;
      }

      input {
        border: 1px solid var(--blue-600);
        border-radius: 5px;
        height: 2rem;
        font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
        font-size: 1.2rem;
      }
    </style>
    <title>Add New</title>
</head>
<body>
    <form action="/games/Edit/<%= title %>" method="post">
        <fieldset>
            <legend>Edit Game</legend>
            
            <input type="text" name="oldTitle" id="title" value="<%= title %>" required hidden>
            <div>
                <label for="title">Title</label>
                <input type="text" name="newTitle" id="title" value="<%= title %>" required>
            </div>

            <div>
                <label for="developer">Developer</label>
                <input type="text" name="newDeveloper" id="developer" value="<%= developer %>" required>
            </div>

            <div>
                <label for="publisher">Publisher</label>
                <input type="text" name="newPublisher" id="publisher" value="<%= publisher %>" required>
            </div>

            <div>
                <label for="genre-select">Genre</label>
                <select name="newGenre" id="genre-select" required>
                    <% for(let i = 0; genres.length > i ; i++) {%>
                        <option value="<%= genres[i] %>"><%= genres[i] %> </option>
                    <% } %>    
                </select>
            </div>

            <button type="submit">Save</button>
        </fieldset>
    </form>
</body>
</html>